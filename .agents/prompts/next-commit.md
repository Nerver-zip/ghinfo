# Plan the Next ghinfo Commit

Read `docs/ROADMAP.md` and `dev/PLAN.md`.

Produce a single-commit plan containing:

- goal;
- non-goals;
- files expected to change;
- observable acceptance criteria;
- tests;
- validation commands;
- compatibility/security considerations.

The commit must remain independently reviewable and reversible.
