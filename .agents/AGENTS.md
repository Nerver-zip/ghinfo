# Agent Directory

Project-wide instructions live in `/AGENTS.md`.

Use this directory for reusable execution knowledge:

- `conventions/`: repository conventions that should remain stable.
- `skills/`: focused procedures for recurring technical work.
- `prompts/`: task prompts that can be handed to implementation/review agents.

Do not duplicate project architecture here. Link back to canonical docs instead.
